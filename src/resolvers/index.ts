import { IResolvers } from '@graphql-tools/utils'

export const resolvers: IResolvers = {
  Query: {
    // Implementation of query resolvers
  },
  Mutation: {
    // Implementation of mutation resolvers
  },
}
